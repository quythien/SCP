% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fourier_sim.R
\name{runFourierDeviationPower}
\alias{runFourierDeviationPower}
\title{Fourier Deviation Simulation}
\usage{
runFourierDeviationPower(
  bio.opts,
  design.opts,
  analysis.opts,
  harmonic_grid = NULL,
  test_type = "DR",
  verbose = TRUE,
  mc.cores = 1L
)
}
\arguments{
\item{bio.opts}{CircadianBioOptions}

\item{design.opts}{CircadianDesignOptions}

\item{analysis.opts}{CircadianAnalysisOptions}

\item{harmonic_grid}{data.frame with columns alpha2, alpha3 (default: 4x3 grid)}

\item{test_type}{"DR", "DP", or "DM"}

\item{verbose}{Print progress}
}
\value{
List with power arrays and summaries across harmonic levels
}
\description{
Tests how power degrades when the true waveform has higher harmonics
(2nd, 3rd) that DCP's cosinor model doesn't fit.
}
\details{
The cosinor model assumes:
  y(t) = M + A*cos(ωt - ωφ) + ε

But the true waveform may be:
  y(t) = M + A*[cos(ωt - ωφ) + α₂*cos(2ωt - ωφ) + α₃*cos(3ωt - ωφ)] + ε

where α₂, α₃ are relative harmonic amplitudes. This module sweeps over
(α₂, α₃) combinations and measures resulting power degradation.
Run Fourier deviation power analysis

Measures how power degrades as higher harmonics are added to the true waveform
while the detection model remains a pure cosinor (fundamental only).
}
