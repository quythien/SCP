% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimation.R
\name{estCircadianParamTwoGroup}
\alias{estCircadianParamTwoGroup}
\title{Estimate a two-group circadian pilot for differential power}
\usage{
estCircadianParamTwoGroup(
  data_1,
  data_2,
  times_1,
  times_2,
  period = 24,
  min_rhythm_pval = 0.01,
  top_k = 300L,
  phase_shift_threshold = 2,
  prop_DM = NULL,
  mesor_diff = NULL,
  dp_shift_mode = c("uniform", "fixed"),
  paired_sigma = FALSE,
  sim.seed = 12345,
  verbose = TRUE
)
}
\arguments{
\item{data_1, data_2}{Gene-by-sample expression matrices for the two groups.}

\item{times_1, times_2}{Numeric collection times for each group.}

\item{period}{Period in hours (default 24).}

\item{min_rhythm_pval}{Marginal p-value screen defining the rhythmic pilot set.}

\item{top_k}{Number of highest-signal genes (per group, ranked by cosinor
p-value) forming each group's effect-size distribution (default 300).
Capped (with a warning) to a group's rhythmic-gene count if larger;
\code{Inf} or \code{"all"} uses every rhythmic candidate.}

\item{phase_shift_threshold}{Minimum phase difference (hours) for the DP class.}

\item{prop_DM}{Optional target proportion of differential-MESOR genes.}

\item{mesor_diff}{Optional MESOR difference for the DM component.}

\item{dp_shift_mode}{Differential-phase shift model, "uniform" or "fixed".}

\item{paired_sigma}{Preserve the paired (amplitude, noise) relationship across groups.}

\item{sim.seed}{Random seed.}

\item{verbose}{Print progress.}
}
\value{
A two-group \code{CircadianBioOptions} pilot summary.
}
\description{
Fits cosinor models to two groups of pilot data and assembles the two-group
pilot summary used by the differential power simulator, encoding
differential rhythmicity (DR), differential phase (DP), and differential
MESOR (DM) structure.
}
