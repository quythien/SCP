% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimation.R
\name{one_cosinor_OLS}
\alias{one_cosinor_OLS}
\title{Fit a Cosinor Model to a Single Gene by OLS}
\usage{
one_cosinor_OLS(tod, y, period = 24, compute.phase.CI = FALSE, CI.level = 0.95)
}
\arguments{
\item{tod}{Numeric vector. Sample time points (hours).}

\item{y}{Numeric vector. Gene expression values (same length as \code{tod}).}

\item{period}{Numeric. Rhythm period in hours (default 24).}

\item{compute.phase.CI}{Logical. Reserved; not yet implemented (default FALSE).}

\item{CI.level}{Numeric. Confidence level for phase CI (default 0.95).}
}
\value{
Named list with fields: \code{M} (mesor), \code{A} (amplitude),
  \code{phi} (acrophase in hours), \code{pvalue} (F-test p-value),
  \code{R2} (coefficient of determination). Returns \code{NA} for all
  fields if the design matrix is singular or \eqn{n \le 3}.
}
\description{
Fits the cosinor model \eqn{y = M + A\cos(\omega t - \phi) + \epsilon}
via ordinary least squares by reparameterising as
\eqn{y = M + \beta_1\cos(\omega t) + \beta_2\sin(\omega t) + \epsilon}.
Returns mesor, amplitude, acrophase, p-value, and R².
}
