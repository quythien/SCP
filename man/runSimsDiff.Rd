% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runner.R
\name{runSimsDiff}
\alias{runSimsDiff}
\title{Low-level differential power simulation engine}
\usage{
runSimsDiff(
  sample_sizes = c(12, 24, 36),
  nsims = 50,
  ngenes = 5000,
  prop_rhythmic = 0.25,
  prop_DR = 0.1,
  prop_DP = 0.1,
  phase_diff = NULL,
  amp_diff = NULL,
  design = c("active", "passive"),
  cts = NULL,
  test_types = c("DR", "DP", "DM"),
  verbose = TRUE,
  harmonics = NULL,
  mc.cores = 1L,
  design.opts = NULL,
  analysis.opts = NULL
)
}
\arguments{
\item{sample_sizes}{\code{CircadianBioOptions} (primary) or numeric vector of
per-group sample sizes (legacy flat API).}

\item{nsims}{\code{CircadianDesignOptions} (primary) or number of simulations (legacy).}

\item{ngenes}{\code{CircadianAnalysisOptions} (primary) or number of genes (legacy).}

\item{prop_rhythmic}{Overall proportion of rhythmic genes (legacy only).}

\item{prop_DR}{Proportion with differential rhythmicity (legacy only).}

\item{prop_DP}{Proportion with differential phase (legacy only).}

\item{phase_diff}{Phase-shift range in hours for DP genes (legacy only).}

\item{amp_diff}{Unused; retained for interface compatibility.}

\item{design}{\code{"active"} or \code{"passive"} (legacy only).}

\item{cts}{TOD distribution for passive design (legacy only).}

\item{test_types}{Which endpoints to evaluate: any of \code{"DR"}, \code{"DP"}, \code{"DM"}.}

\item{verbose}{Print progress.}

\item{mc.cores}{Parallel cores.}

\item{design.opts}{\code{CircadianDesignOptions} (used with legacy positional first arg).}

\item{analysis.opts}{\code{CircadianAnalysisOptions}.}
}
\value{
List (class \code{SCPDiffResult}) with fields:
  \code{pval_DR}, \code{fdr_DR}, \code{pval_DP}, \code{fdr_DP},
  \code{pval_DM}, \code{fdr_DM} -- arrays \code{[ngenes x n_sizes x nsims]};
  \code{diff_type}, \code{effectsize}, \code{sample_sizes}, \code{nsims}.
  Pass directly to \code{plotDiffPower()} or \code{npower()}.
}
\description{
Preferred interface: \code{runDifferentialPower(bio.opts, design.opts, analysis.opts)}.
This function supports both the config-object API (primary) and a legacy
flat-argument API (retained for back-compatibility).
}
\seealso{
\code{\link{runDifferentialPower}} (user-facing wrapper)
}
