% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detection.R
\name{DCP_Analyze}
\alias{DCP_Analyze}
\title{Complete Differential Circadian Analysis}
\usage{
DCP_Analyze(
  expr1,
  expr2,
  times1,
  times2,
  alpha = 0.05,
  gene_names = NULL,
  parallel.ncores = 1
)
}
\arguments{
\item{expr1}{Expression matrix for group 1 (genes x samples)}

\item{expr2}{Expression matrix for group 2 (genes x samples)}

\item{times1}{Time points for group 1}

\item{times2}{Time points for group 2}

\item{alpha}{Significance threshold (default 0.05)}

\item{gene_names}{Gene names (optional)}

\item{parallel.ncores}{Number of cores for parallel processing}
}
\value{
List with:
  \item{DR}{Differential rhythmicity results}
  \item{DP}{Differential phase results}
  \item{classification}{Gene classifications (DR/DP/NS)}
}
\description{
Performs differential tests (DR, DP) with classification
}
\examples{
x = DCP_sim_data(ngene=1000, nsample=30, A1=c(1, 3), A2=c(1, 3),
phase1=c(0, pi/4), phase2=c(pi/4, pi/2),
M1=c(4, 6), M2=c(4, 6), sigma1=1, sigma2=1)
dcp_result = DCP_Analyze(x[[1]]$data, x[[2]]$data,
                       x[[1]]$time, x[[2]]$time)

}
