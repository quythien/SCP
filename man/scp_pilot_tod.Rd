% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pilot_database.R
\name{scp_pilot_tod}
\alias{scp_pilot_tod}
\title{Get the time-of-day (TOD) vector for a bundled SCP pilot}
\usage{
scp_pilot_tod(species, dataset, tissue, condition = NULL)
}
\arguments{
\item{species, dataset, tissue, condition}{manifest tuple identifying the
pilot.}
}
\value{
Numeric vector of times in hours (mod 24); \code{NULL} if no
  TOD information is available for this pilot.
}
\description{
Returns the per-sample sampling times for a pilot, in hours mod 24.
Prefers the raw \code{p$times} field when the pilot rds preserved it;
otherwise parses the manifest \code{tod_phases} column and returns the
unique sampling phases.
}
\examples{
\dontrun{
  tod <- scp_pilot_tod("baboon", "GSE98965", "SCN", "All")
  hist(tod, breaks = seq(0, 24, by = 1))
}
}
