% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize_dcp_pairs.R
\name{summarizeDCPPairs}
\alias{summarizeDCPPairs}
\title{Summarize DCP pair results into a wide gene-level table}
\usage{
summarizeDCPPairs(
  rds_paths,
  pair_meta = NULL,
  out_csv = NULL,
  min_n_recommend = 50L,
  p_thresholds = c(0.05, 0.01, 0.001),
  fdr_thresholds = c(0.2, 0.1, 0.05, 0.01),
  top_k = 300L,
  species = "Human",
  dataset = ""
)
}
\arguments{
\item{rds_paths}{Named character vector: names = pair tags, values = RDS paths.}

\item{pair_meta}{Data frame with columns: tag, tissue1, tissue2.}

\item{out_csv}{Path for output CSV (NULL = return only).}

\item{min_n_recommend}{Minimum gene count to flag endpoint as sufficient (default 50).}

\item{p_thresholds}{Raw p-value thresholds (default c(0.05, 0.01, 0.001)).}

\item{fdr_thresholds}{FDR thresholds (default c(0.20, 0.10, 0.05, 0.01)).}

\item{top_k}{Number of top rhythmic genes (by p-value) for r statistics (default 300).}

\item{species}{Character string for species column.}

\item{dataset}{Character string for dataset column.}
}
\value{
Data frame, one row per pair, sorted by sufficient_all3 then score_all3.
}
\description{
Loads saved DCP_Analyze RDS files and computes a wide-format table with:
  - Sample and gene counts per tissue
  - TOD coverage per tissue
  - Single-tissue rhythmicity at multiple p/FDR thresholds
  - Joint rhythmicity (TOJR) breakdown
  - DR/DP/DA/DM counts at multiple thresholds
  - r = A/sigma for top-300 rhythmic genes (cross-tissue comparable)
  - Effect sizes: median |delta_phi| (DP), median |delta_M| (DM)
  - Recommendation flags (>= min_n_recommend genes per endpoint at FDR 5%)
}
