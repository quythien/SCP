% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimation.R
\name{adjust.to.2pi}
\alias{adjust.to.2pi}
\title{Estimate Circadian Parameters from Pilot Data}
\usage{
adjust.to.2pi(x)
}
\arguments{
\item{x}{Numeric. Angle(s) in radians.}

\item{data}{Gene expression matrix (genes x samples).}

\item{times}{Numeric vector of sample time points (hours), length = \code{ncol(data)}.}

\item{period}{Circadian period in hours (default 24).}

\item{min_rhythm_pval}{P-value threshold for classifying a gene as rhythmic (default 0.01).}

\item{verbose}{Print estimation summary (default TRUE).}
}
\value{
A \code{CircadianBioOptions} S3 object (27 fields) including:
  \code{prop_rhythmic}, \code{lBaselineExpr}, \code{lOD}, \code{amplitude},
  \code{sigma_rhythmic}, \code{cts}, \code{phase}, \code{prop_DR},
  \code{prop_DP}, \code{prop_DM}, \code{phase_diff}, \code{amp_diff},
  \code{mesor_diff}, \code{ngenes}, \code{period}, \code{sim.seed}.
  Pass directly to \code{runSimsSingleCohort()} or \code{runDifferentialPower()}.

Numeric. Angle(s) mapped to \code{[0, 2π)}.
}
\description{
Fits a cosinor model to each gene in the pilot dataset, selects the
top-K rhythmic genes (ranked by p-value, K = min(300, n_rhythmic)),
and returns empirical distributions of mesor, amplitude, phase, and
noise as a \code{CircadianBioOptions} object for use in downstream
simulation and power analysis.
}
\seealso{
\code{\link{estCircadianParamTwoGroup}} for two-group differential setup,
  \code{\link{CircadianBioOptions}} for the options constructor.
Wrap Angle to [0, 2π)
}
