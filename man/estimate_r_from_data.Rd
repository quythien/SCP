% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{estimate_r_from_data}
\alias{estimate_r_from_data}
\title{Estimate Signal-to-Noise Ratio (r) from DCP Results}
\usage{
estimate_r_from_data(dcp_results, top_n = 100, rank_by = "q.R2", use_group = 1)
}
\arguments{
\item{dcp_results}{List from DCP analysis containing DR and DP components}

\item{top_n}{Number of top circadian genes to use (default: 100)}

\item{rank_by}{Column to rank genes by (default: "p.overall" from DP results)}

\item{group}{Which group to use for R^2 ("min" for minimum across groups, "1" or "2" for specific group)}
}
\value{
List with median r, mean r, and individual r values
}
\description{
Estimate the median signal-to-noise ratio r = A/sigma from the
top circadian genes in a DCP (Differential Circadian Pipeline) analysis.
Uses the relationship r = 1/sqrt(1/R^2 - 1) where R^2 is rhythm strength.
}
\examples{
\dontrun{
# dcp_results is the output from runDiffCircadian()
r_estimate <- estimate_r_from_data(dcp_results, top_n = 100)
# Use median r for power simulation
target_r <- r_estimate$median_r
}

}
