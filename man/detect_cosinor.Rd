% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detection.R
\name{detect_cosinor}
\alias{detect_cosinor}
\title{Unified cosinor rhythmicity detection (single- or multi-harmonic)}
\usage{
detect_cosinor(expr, times, K = 1L, period = 24, ebayes = FALSE, mc.cores = 1L)
}
\arguments{
\item{expr}{Gene x sample expression matrix}

\item{times}{Numeric time vector (length = ncol(expr))}

\item{K}{Harmonic order (default 1)}

\item{period}{Period in hours (default 24)}

\item{ebayes}{For \code{K >= 2}, moderate residual variance with
empirical-Bayes shrinkage (default \code{FALSE}, the unshrunk exact
F-test)}

\item{mc.cores}{Cores for the \code{K >= 2} fit (default 1)}
}
\value{
Numeric vector of raw p-values, length \code{nrow(expr)}
}
\description{
Canonical entry point for rhythmicity detection by K-harmonic cosinor
regression. \code{K = 1} is the single-harmonic cosinor F-test, identical
to \code{\link{detect_DCP}}; \code{K = 2} adds the 12-hour second harmonic
and tests the joint harmonic block. By default the unshrunk exact nested
F-test is used (matching the power derivations in the paper). Returns raw
p-values; the caller applies FDR control.
}
\seealso{
\code{\link{detect_DCP}}
}
