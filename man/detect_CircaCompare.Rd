% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detection.R
\name{detect_CircaCompare}
\alias{detect_CircaCompare}
\title{CircaCompare Differential Analysis Wrapper}
\usage{
detect_CircaCompare(
  expr1,
  times1,
  expr2,
  times2,
  gene_names = NULL,
  period = 24,
  alpha_threshold = 0.05
)
}
\arguments{
\item{expr1}{Expression matrix for group 1 (genes x samples)}

\item{times1}{Time points for group 1}

\item{expr2}{Expression matrix for group 2 (genes x samples)}

\item{times2}{Time points for group 2}

\item{gene_names}{Gene name vector (optional)}

\item{period}{Circadian period (default 24)}

\item{alpha_threshold}{Significance level for per-group rhythmicity (default 0.05)}
}
\value{
List with pval_DR (NA -- no equivalent), pval_DP, pval_DM
        Each is a numeric vector of length ngenes; default 1 for failed/untested.
}
\description{
Per-gene NLS fitting with group-interaction parameters.
Model: y ~ (k + k1*group) + (alpha + alpha1*group) * cos(tau*t - (phi + phi1*group))
Provides separate Wald t-tests for amplitude diff (alpha1 -> DA),
phase diff (phi1 -> DP), and mesor diff (k1 -> DM).
}
\details{
CircaCompare calls stop() when either group is arrhythmic, so every
         gene call is wrapped in tryCatch. NLS failures also yield p=1 (conservative).
         CircaCompare has no differential rhythmicity (DR) test -- only per-group
         rhythmicity p-values. pval_DR is set to NA for all genes.
}
