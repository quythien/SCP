% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_diff.R
\name{plotDiffPower}
\alias{plotDiffPower}
\title{Plot differential circadian power}
\usage{
plotDiffPower(
  res_list,
  comp_labels = names(res_list),
  endpoints = c("DR", "DP", "DM"),
  fdr_thresholds = c(0.01, 0.05, 0.1, 0.2),
  panel_fdr = 0.05,
  vline_power = 0.8,
  vline_fdr = 0.2,
  display_sizes = NULL,
  stratified = FALSE,
  r_break_width = 0.25,
  r_max = 5,
  r_display_max = NULL,
  main_title = "Differential Circadian Power Analysis",
  out_pdf = NULL,
  width = NULL,
  height = NULL
)
}
\arguments{
\item{res_list}{Named list of length 2, each element output of runSimsDiff().}

\item{comp_labels}{Character(2), display labels for the two comparisons.}

\item{endpoints}{Which endpoints to include (default c("DR","DP","DM")).}

\item{fdr_thresholds}{FDR levels for panel A (default c(0.01,0.05,0.10,0.20)).}

\item{panel_fdr}{FDR threshold used to draw the marginal power curve in
the non-stratified layout (default 0.05).}

\item{vline_power}{Horizontal reference line for target power, drawn at
this value (default 0.80).}

\item{vline_fdr}{FDR threshold marked with a reference guide on the
power panels (default 0.20).}

\item{display_sizes}{Sample sizes to show in all panels (NULL = all).}

\item{stratified}{Logical (default FALSE). If FALSE, draws only the
marginal power-vs-sample-size panel per endpoint (1 x n_endpoints layout
per comparison). If TRUE, also draws power-by-r and true-discoveries-by-r
strata panels in the original 3-column layout.}

\item{r_break_width}{Width of r-strata bins (default 0.25). Ignored if
stratified = FALSE.}

\item{r_max}{Upper bound (in r = A/sigma units) for the r-strata
breakpoints when \code{stratified = TRUE} (default 5).}

\item{r_display_max}{Optional upper x-axis limit (r units) for the
stratified panels; \code{NULL} (default) uses the full stratified range.}

\item{main_title}{Outer plot title (default
\code{"Differential Circadian Power Analysis"}).}

\item{out_pdf}{Path for PDF. If NULL, plots to current device.}

\item{width}{PDF width in inches. Defaults adapt to stratified.}

\item{height}{PDF height in inches. Defaults adapt to stratified.}
}
\value{
Invisibly, list of panel data per comparison/endpoint.
}
\description{
Plots two-group differential power for one or more comparisons from the
objects returned by \code{\link{runDifferentialPower}}. By default it
draws marginal power against sample size, with one column per endpoint
and one row per comparison. Setting \code{stratified = TRUE} adds the
effect-size-stratified panels (power and true discoveries by effect size)
in a three-column layout.
}
\examples{
\donttest{
praw <- readRDS(system.file("extdata", "example_pilot_raw.rds",
                            package = "SCP"))
e1 <- praw$adrenal$expr[1:300, ]; t1 <- praw$adrenal$times
e2 <- praw$liver$expr[1:300, ];   t2 <- praw$liver$times
bio  <- estCircadianParamTwoGroup(e1, e2, t1, t2, top_k = 100,
                                  verbose = FALSE)
bio$ngenes <- 200L
dopt <- CircadianDesignOptions(sample_sizes = c(24, 48), nsims = 5)
aopt <- CircadianAnalysisOptions(alpha = 0.05)
res  <- runDifferentialPower(bio, dopt, aopt, test_types = "DR",
                             plot = FALSE, verbose = FALSE)
plotDiffPower(list(res), endpoints = "DR",
              out_pdf = tempfile(fileext = ".pdf"))
}
}
