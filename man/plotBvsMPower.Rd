% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_bvsm.R
\name{plotBvsMPower}
\alias{plotBvsMPower}
\title{=======================================================================
plot_bvsm.R — B vs m Trade-off Plot (Figure 3)
=======================================================================}
\usage{
plotBvsMPower(
  x,
  nsims = 30L,
  alpha2 = 0,
  fdr_power = 0.8,
  dataset_labels = NULL,
  output_file = NULL,
  width = 14,
  height = 5
)
}
\arguments{
\item{x}{An \code{SCPSingleResult} from \code{runSingleCohortGrid()},
or a tidy \code{data.frame} with columns
\code{N, B, power, power_se, dataset} (and optionally
\code{alpha2}).}

\item{nsims}{Number of simulation replicates used (to convert SE → SD).
Default 30.}

\item{alpha2}{Which \code{alpha2} value to plot (default 0 = pure cosinor).}

\item{fdr_power}{Reference power level for dashed line (default 0.80).}

\item{dataset_labels}{Named character vector mapping dataset codes to display
labels. If NULL, uses dataset column values directly.}

\item{output_file}{Path to save PDF. NULL = current graphics device.}

\item{width}{PDF width in inches (default 14).}

\item{height}{PDF height in inches (default 5).}
}
\value{
Invisibly returns the ggplot object.
}
\description{
Produces a faceted power-vs-N figure for each dataset in the result,
with one line per B value. SD bars (±1 SD across simulation replicates)
are drawn at each N. B-invariance appears as overlapping lines.
}
\details{
Plots DCP power vs N for multiple B values, demonstrating B-invariance.
Input: SCPSingleResult from runSingleCohortGrid() or a tidy data.frame
with columns N, B, alpha2, power, power_se.
Plot B vs m Trade-off Power Grid
}
