% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{simCircadianSingleCohort2H}
\alias{simCircadianSingleCohort2H}
\title{Simulate Single-Cohort Circadian Data Under the Two-Harmonic Cosinor Model}
\usage{
simCircadianSingleCohort2H(bio.opts, cts, seed = NULL)
}
\arguments{
\item{bio.opts}{A \code{CircadianBioOptions} produced by
\code{\link{estCircadianParam2H}} (must carry \code{paired_2h = TRUE},
\code{amplitude2}, \code{phase2}).}

\item{cts}{Numeric vector of sample collection times in hours; length
determines \eqn{N}.}

\item{seed}{Optional integer random seed.}
}
\value{
Named list:
  \describe{
    \item{\code{expr}}{Gene expression matrix (\code{ngenes} x \eqn{N}).}
    \item{\code{is_rhythmic}}{Logical vector.}
    \item{\code{r_values}}{Per-gene 1st-harmonic SNR \eqn{A_{g,1}/\sigma_g}.}
    \item{\code{A1, phi1, A2, phi2}}{Per-gene truth vectors (length
      \code{ngenes}; zero for non-rhythmic genes).}
  }
}
\description{
Two-harmonic analog of \code{\link{simCircadianSingleCohort}}.
Generates expression from
\deqn{y_{gi} = \mu_g + A_{g,1} \cos(\omega_0 (t_i - \phi_{g,1}))
             + A_{g,2} \cos(2 \omega_0 (t_i - \phi_{g,2})) + \epsilon_{gi}}
using the joint pilot tuple \eqn{(A_1, \phi_1, A_2, \phi_2, \sigma)} stored
on \code{bio.opts} by \code{\link{estCircadianParam2H}}. The same index is
drawn across all five vectors so the pilot's cross-parameter dependence is
preserved.

\code{r_values} is reported on the 1st-harmonic scale \eqn{r_g = A_{g,1} / \sigma_g}
to match the existing r-stratification machinery and the manuscript's
\eqn{r_g} notation.
}
\seealso{
\code{\link{estCircadianParam2H}}, \code{\link{simCircadianSingleCohort}}
}
