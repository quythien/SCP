% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_diff.R
\name{.collapseTail}
\alias{.collapseTail}
\title{Collapse High-r Tail Strata into a Single Bin}
\usage{
.collapseTail(
  r_breaks,
  strata_labels,
  gene_counts,
  mean_TD,
  se_TD,
  mean_pow,
  se_pow,
  threshold = 5
)
}
\arguments{
\item{r_breaks}{Numeric vector. Stratum breakpoints (length = n_strata + 1).}

\item{strata_labels}{Character vector. Labels for each stratum.}

\item{gene_counts}{Integer vector. Gene counts per stratum per simulation.}

\item{mean_TD}{Numeric vector. Mean true discoveries per stratum.}

\item{se_TD}{Numeric vector. SE of true discoveries per stratum.}

\item{mean_pow}{Numeric vector. Mean power per stratum.}

\item{se_pow}{Numeric vector. SE of power per stratum.}

\item{threshold}{Numeric. r value above which strata are collapsed (default 5).}
}
\value{
Named list with collapsed vectors: \code{strata_labels},
  \code{gene_counts}, \code{mean_TD}, \code{se_TD}, \code{mean_pow},
  \code{se_pow}, and scalar \code{n_strata}.
}
\description{
Merges all r-strata whose left boundary is at or above \code{threshold}
into a single "r >= threshold" bin. This prevents sparse high-r strata
from cluttering stratified differential power plots.
}
